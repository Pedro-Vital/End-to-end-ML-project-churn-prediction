global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "churn_api"
    static_configs:
      - targets: ["churn_api:8000"]
      # This part must match the service name and port of the FastAPI app in docker-compose.yaml
      # Containers are reachable by their service names (Docker DNS) within the Docker network
