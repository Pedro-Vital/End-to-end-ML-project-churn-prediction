artifacts_root: artifacts

data_ingestion:
  root_dir: artifacts/data_ingestion
  db_host: "localhost"
  db_user: "pedro"
  db_password: "12345"
  db_name: "bank_db"
  query: "SELECT * FROM churners;"
